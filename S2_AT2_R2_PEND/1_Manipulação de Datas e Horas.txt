//1_Data:

let data = new Date()
function obterDataAtual(data){
    console.log(data.getDate(), "/", data.getMonth()+1, "/", data.getFullYear());
 }
obterDataAtual(data);

//1.2_Hora:

let hora = new Date()
function obterHoraAtual() {
    console.log(hora.getHours()-3, ":" ,hora.getMinutes(), ":" ,hora.getSeconds())
}
obterHoraAtual(hora);